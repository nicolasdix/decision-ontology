# SOC to Turtle Converter

This script converts PrefLib Strict Orders Complete (`.soc`) files into
RDF/Turtle format using the Decision Ontology.

## Requirements
- Python 3.11
- No external dependencies

## Usage
```bash
python soc_to_ontology.py path/to/file.soc
